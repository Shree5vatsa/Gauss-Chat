# Express User Type Declaration

Extends Express `req.user` with our Mongoose `UserDocument` type.
This code merges our custom UserDocument interface (from Mongoose) into Express's built-in User interface.